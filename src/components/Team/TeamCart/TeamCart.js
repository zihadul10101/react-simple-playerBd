import React from 'react';

const TeamCart = (props) => {
   // const selectPlayer =props.selectPlayer;
    return (
        <div>
             <h2>This is team:{props.selectPlayer.length}</h2>  
        </div>
    );
};

export default TeamCart;